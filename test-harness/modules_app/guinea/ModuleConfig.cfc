/**
 * The guinea (pig) fixture module: a tiny protected target for the OriginFirewall
 * integration specs. It has no behavior of its own.
 */
component {

	this.title = "guinea";

	/**
	 * Configure Module
	 */
	function configure(){
	}

}
